.venv/
__pycache__/
*.pyc
# local runtime state: sqlite db, telegram session, google service account key
# (secrets/private keys — never commit any of this, whatever the filename)
backend/data/
.env
*.env
!.env.example
.DS_Store
node_modules/


# oh-my-claudecode plugin state
.omc/

# claude code local state (keep launch.json — useful preview/dev-server config)
.claude/*
!.claude/launch.json
